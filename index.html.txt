<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dave’s Pizza — Midway, Utah</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .fade-in{opacity:0;transform:translateY(6px);animation:fi .5s ease forwards}
    @keyframes fi{to{opacity:1;transform:none}}
    .drawer-enter{transform:translateX(100%)}.drawer-enter.show{transform:translateX(0)}

    /* Brand palette extracted from your logo */
    :root{
      --brand-red:#9d292d;        /* deep red */
      --brand-red-dark:#852226;   /* hover red */
      --brand-green:#074431;      /* deep green */
      --brand-green-dark:#053929; /* darker green */
    }
    /* Minimal utilities to avoid layout shifts */
    .btn-brand{background:var(--brand-red);color:#fff}
    .btn-brand:hover{background:var(--brand-red-dark)}
    .btn-green{background:var(--brand-green);color:#fff}
    .btn-green:hover{background:var(--brand-green-dark)}
    .text-brand{color:var(--brand-red)}
    .text-green-brand{color:var(--brand-green)}
    .brand-nav a{color:#000}
    .brand-nav a:hover{color:var(--brand-red)}
    .focus-green:focus{outline:none;box-shadow:0 0 0 2px rgba(7,68,49,.35)}
  </style>
</head>
<body class="min-h-screen bg-white text-slate-900">
  <!-- Header -->
  <header class="sticky top-0 z-40 backdrop-blur supports-[backdrop-filter]:bg-white/70 bg-white/90 border-b border-slate-200">
    <div class="mx-auto max-w-7xl px-4 h-16 flex items-center justify-between">
      <div class="flex items-center gap-2 select-none" aria-label="Dave's Pizza logo">
        <img id="headerLogo" src="logo.png" alt="Dave's Pizza logo" class="h-10 w-auto object-contain" onerror="this.style.display='none'">
        <span class="font-extrabold tracking-tight text-lg sm:text-xl">Dave’s Pizza</span>
      </div>
      <nav class="hidden md:flex items-center gap-6 brand-nav" aria-label="Primary">
        <a href="#home" class="">Home</a>
        <a href="#menu" class="">Menu</a>
        <a href="#about" class="">About</a>
        <a href="#reviews" class="">Reviews</a>
        <a href="#faq" class="">FAQ</a>
        <a href="#contact" class="">Contact</a>
        <a href="#blog" class="">Blog</a>
      </nav>
      <div class="flex items-center gap-2">
        <button id="cartBtn" class="inline-flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold btn-brand text-white shadow-sm" aria-haspopup="dialog">
          <span>View Cart</span>
          <span id="cartCount" class="inline-flex items-center justify-center w-6 h-6 rounded-lg bg-white/20">0</span>
        </button>
        <button id="navBtn" class="md:hidden p-2" aria-label="Toggle navigation">
          <svg width="22" height="22" viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
        </button>
      </div>
    </div>
    <div id="mobileNav" class="hidden md:hidden border-t border-slate-200">
      <div class="px-4 py-3 flex flex-col gap-3">
        <a href="#home" class="py-1">Home</a>
        <a href="#menu" class="py-1">Menu</a>
        <a href="#about" class="py-1">About</a>
        <a href="#reviews" class="py-1">Reviews</a>
        <a href="#faq" class="py-1">FAQ</a>
        <a href="#contact" class="py-1">Contact</a>
        <a href="#blog" class="py-1">Blog</a>
      </div>
    </div>
  </header>

  <!-- Hero -->
  <section id="home" class="relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <!-- Two-color split background (red top, green bottom), lightly faded -->
      <div class="w-full h-full opacity-20" style="background:linear-gradient(180deg, var(--brand-red) 0%, var(--brand-red) 50%, var(--brand-green) 50%, var(--brand-green) 100%);"></div>
    </div>
    </div>
    <div class="mx-auto max-w-7xl px-4 py-10 sm:py-8 grid md:grid-cols-2 gap-8 items-center relative z-10">
      <div>
        <h1 class="text-3xl sm:text-6xl font-extrabold tracking-tight fade-in"><span class="text-green-brand">Wood‑Fired. Handcrafted.</span> <span class="text-brand">Dave’s Pizza.</span> </h1>
        <p class="mt-10 sm:mt-15 text-slate-600 max-w-prose">Old‑school techniques, local ingredients, served fast with a smile. Order online or swing by the shop—your next favorite slice is a click away.</p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a href="#menu" class="rounded-xl px-5 py-3 text-sm font-semibold btn-brand text-white shadow-md">Order Now</a>
          <a href="#about" class="rounded-xl px-5 py-3 text-sm font-semibold btn-green text-white shadow-md">Our Story</a>
        </div>
        <div class="mt-6 flex items-center gap-4 text-sm text-slate-600">

          
          <div>• Mon–Thu 11:00 AM–10:00 PM • Fri–Sat 11:00 AM–11:00 PM • Sun Closed</div>
        </div>
      </div>
      <div class="relative flex justify-center items-center">
        <img id="heroLogo" src="logo.png" alt="Dave's Pizza" class="w-full max-w-sm mx-auto rounded-3xl shadow-xl border bg-white/80 p-6" onerror="this.style.display='none'">
      </div>
    </div>
  </section>

  <!-- Menu -->
  <section id="menu" class="scroll-mt-24 py-8 sm:py-10" aria-labelledby="menu-title">
    <div class="mx-auto max-w-7xl px-4">
      <div class="mb-5">
        <h2 id="menu-title" class="text-2xl sm:text-3xl font-bold tracking-tight">Menu</h2>
        <p class="text-slate-600 mt-2 max-w-3xl">Simple, focused, and fired to perfection.</p>
      </div>
      <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3 justify-between">
        <div class="relative w-full sm:w-96">
          <input id="search" placeholder="Search menu (e.g., pepperoni, dipping)" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus:outline-none focus:ring-2 focus-green" aria-label="Search the menu"/>
          <div class="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">🔎</div>
        </div>
        <p class="text-sm text-slate-600">Wood‑fired 12&quot; pies • Gluten‑free crust +$2 • Extra cheese +$2</p>
      </div>
      <div id="menuGrid" class="mt-8 grid sm:grid-cols-2 lg:grid-cols-3 gap-6"></div>
    </div>
  </section>

  <!-- About -->
  <section id="about" class="scroll-mt-24 py-8 sm:py-10" aria-labelledby="about-title">
    <div class="mx-auto max-w-7xl px-4">
      <div class="mb-5">
        <h2 id="about-title" class="text-2xl sm:text-3xl font-bold tracking-tight">About Us</h2>
        <p class="text-slate-600 mt-2 max-w-3xl">Story, mission, and values that guide every slice.</p>
      </div>
      <div class="grid lg:grid-cols-3 gap-8 items-start">
        <div class="lg:col-span-2">
          <p class="text-slate-700 leading-relaxed"><strong>I’m Dave, the founder of Dave’s Pizza in Midway, Utah.</strong> We make wood-fired, handcrafted pizzas using local ingredients and old-school techniques — served fast, with a smile. Passion for quality is how we do business.</p>
          <ul class="mt-6 space-y-3 text-slate-700 list-disc pl-5">
            <li><strong>Clear Branding & First Impression.</strong> Clean, modern design; consistent logo/colors/fonts; a clear tagline so visitors instantly get what we do.</li>
            <li><strong>Easy Navigation.</strong> Simple menu with 6 main sections, strong CTA buttons, and a search bar for quick finds.</li>
            <li><strong>Essential Pages.</strong> Home, About, Menu (Products), Contact (form/email/phone/map), Reviews, FAQ.</li>
            <li><strong>Trust & Credibility.</strong> Real reviews, pro visuals, and transparent policies.</li>
            <li><strong>Conversion Tools.</strong> Bold CTAs, short forms, cart, and a secure-feel checkout flow.</li>
            <li><strong>Technical Quality.</strong> Fast, responsive, SEO‑friendly structure, and accessible UI.</li>
            <li><strong>Extras.</strong> Blog/resources, newsletter signup, and a quick chat widget.</li>
          </ul>
        </div>
        <div class="rounded-2xl border border-slate-200 p-5 shadow-sm bg-white">
          <h3 class="font-semibold">Branding Quick Facts</h3>
          <ul class="mt-2 text-sm text-slate-600 space-y-1">
            <li><span class="font-medium">Logo:</span> Your red/green wordmark.</li>
            <li><span class="font-medium">Colors:</span> Amber accents + Slate neutrals.</li>
            <li><span class="font-medium">Tone:</span> Friendly, local, quality‑driven.</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Reviews -->
  <section id="reviews" class="scroll-mt-24 py-8 sm:py-10">
    <div class="mx-auto max-w-7xl px-4">
      <div class="mb-5">
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Testimonials & Reviews</h2>
        <p class="text-slate-600 mt-2 max-w-3xl">Social proof that builds trust.</p>
      </div>
      <div class="grid md:grid-cols-3 gap-6">
        <figure class="rounded-2xl border border-slate-200 p-5 bg-white shadow-sm"><div class="text-brand">⭐⭐⭐⭐⭐</div><blockquote class="mt-2 text-slate-700">“The crust has that perfect wood‑fired chew. Pepperoni is elite.”</blockquote><figcaption class="mt-3 text-sm text-slate-500">— Maya P.</figcaption></figure>
        <figure class="rounded-2xl border border-slate-200 p-5 bg-white shadow-sm"><div class="text-brand">⭐⭐⭐⭐⭐</div><blockquote class="mt-2 text-slate-700">“Fast, friendly, and the buffalo chicken is my weekly go‑to.”</blockquote><figcaption class="mt-3 text-sm text-slate-500">— Kaden L.</figcaption></figure>
        <figure class="rounded-2xl border border-slate-200 p-5 bg-white shadow-sm"><div class="text-brand">⭐⭐⭐⭐⭐</div><blockquote class="mt-2 text-slate-700">“BBQ chicken + ranch cup = chef’s kiss. Love the local vibes.”</blockquote><figcaption class="mt-3 text-sm text-slate-500">— Riley S.</figcaption></figure>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="scroll-mt-24 py-8 sm:py-10">
    <div class="mx-auto max-w-7xl px-4">
      <h2 class="text-2xl sm:text-3xl font-bold tracking-tight mb-5">FAQ</h2>
      <div class="grid lg:grid-cols-2 gap-6">
        <details class="rounded-2xl border border-slate-200 p-4 bg-white"><summary class="font-medium cursor-pointer">Do you offer gluten‑free crust?</summary><p class="mt-2 text-slate-600">Yes! Add $2 for a 12" gluten‑free crust. We prep carefully, but our kitchen is not celiac‑certified.</p></details>
        <details class="rounded-2xl border border-slate-200 p-4 bg-white" open><summary class="font-medium cursor-pointer">What are your hours?</summary><p class="mt-2 text-slate-600">Mon–Thu 11:00 AM–10:00 PM • Fri–Sat 11:00 AM–11:00 PM • Sun Closed.</p></details>
        <details class="rounded-2xl border border-slate-200 p-4 bg-white"><summary class="font-medium cursor-pointer">Do you cater or handle large orders?</summary><p class="mt-2 text-slate-600">Absolutely—email us or call and we’ll set you up.</p></details>
        <details class="rounded-2xl border border-slate-200 p-4 bg-white"><summary class="font-medium cursor-pointer">Where are you located?</summary><p class="mt-2 text-slate-600">Right in Midway, Utah. See the map below for directions!</p></details>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="scroll-mt-24 py-8 sm:py-10">
    <div class="mx-auto max-w-7xl px-4">
      <div class="mb-5">
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Contact & Visit</h2>
        <p class="text-slate-600 mt-2 max-w-3xl">Reach out or swing by—we’d love to feed you.</p>
      </div>
      <div class="grid lg:grid-cols-2 gap-8 items-start">
        <form id="contactForm" class="rounded-2xl border border-slate-200 p-6 bg-white shadow-sm" aria-label="Contact form">
          <div class="grid sm:grid-cols-2 gap-4">
            <label class="block"><span class="text-xs font-medium text-slate-600">Name</span><input required class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
            <label class="block"><span class="text-xs font-medium text-slate-600">Email</span><input type="email" required class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
          </div>
          <label class="block mt-4"><span class="text-xs font-medium text-slate-600">Message</span><textarea required rows="4" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></textarea></label>
          <button class="mt-4 rounded-xl px-5 py-3 text-sm font-semibold btn-brand text-white shadow-sm">Send Message</button>
          <p class="mt-4 text-sm text-slate-600">Or email <a class="underline" href="mailto:hello@davespizza.example">hello@davespizza.example</a> • Call (435) 555‑0123</p>
        </form>
        <div class="space-y-4">
          <div class="rounded-2xl overflow-hidden border border-slate-200 shadow-sm bg-slate-50 aspect-video">
            <iframe title="Map of Midway, Utah" class="w-full h-full" loading="lazy" referrerpolicy="no-referrer-when-downgrade" src="https://www.google.com/maps?q=Midway%2C%20Utah&output=embed"></iframe>
          </div>
          <div class="rounded-2xl border border-slate-200 p-4 bg-white shadow-sm text-sm text-slate-600">123 Main St, Midway, UT • Parking behind building • Family friendly • Card & contactless accepted</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Blog -->
  <section id="blog" class="py-12 bg-brand-tint border-y border-brand-tint">
    <div class="mx-auto max-w-7xl px-4 flex flex-col md:flex-row items-center justify-between gap-6">
      <div>
        <h3 class="text-xl font-bold">Join our newsletter</h3>
        <p class="text-slate-600">Deals, new pies, and local stories—no spam.</p>
      </div>
      <form id="newsletter" class="flex w-full md:w-auto gap-2">
        <input type="email" required placeholder="you@example.com" class="flex-1 md:w-80 rounded-xl border border-slate-300 px-4 py-3 focus-green" aria-label="Email">
        <button class="rounded-xl px-5 py-3 text-sm font-semibold btn-green text-white shadow-sm">Subscribe</button>
      </form>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-10 text-white" style="background: var(--brand-green)">
    <div class="mx-auto max-w-7xl px-4 grid sm:grid-cols-2 lg:grid-cols-4 gap-6 text-sm text-white/85">
      <div>
        <div class="flex items-center gap-2 select-none" aria-label="Dave's Pizza logo">
          <img id="footerLogo" src="logo.png" alt="Dave's Pizza logo" class="h-10 w-auto object-contain" onerror="this.style.display='none'">
          <span class="font-extrabold tracking-tight text-lg sm:text-xl">Dave’s Pizza</span>
        </div>
        <p class="mt-3 max-w-xs">Handcrafted pies in Midway, UT. © <span id="year"></span> Dave’s Pizza.</p>
      </div>
      <div>
        <h4 class="font-semibold text-white">Explore</h4>
        <ul class="mt-2 space-y-1">
          <li><a href="#menu" class="hover:underline">Menu</a></li>
          <li><a href="#about" class="hover:underline">About</a></li>
          <li><a href="#reviews" class="hover:underline">Reviews</a></li>
          <li><a href="#faq" class="hover:underline">FAQ</a></li>
        </ul>
      </div>
      <div>
        <h4 class="font-semibold text-white">Legal</h4>
        <ul class="mt-2 space-y-1">
          <li><a href="#" onclick="alert('Privacy Policy: we only use form inputs to contact you back. No tracking here in the demo.')" class="hover:underline">Privacy</a></li>
          <li><a href="#" onclick="alert('Terms of Service: demo site, not a real ordering system. Prices subject to change.')" class="hover:underline">Terms</a></li>
        </ul>
      </div>
      <div>
        <h4 class="font-semibold text-white">Follow</h4>
        <p class="mt-2">@davespizza (Instagram • Facebook)</p>
      </div>
    </div>
  </footer>

  <!-- Cart Drawer -->
  <div id="overlay" class="fixed inset-0 bg-black/40 hidden"></div>
  <aside id="drawer" class="fixed right-0 top-0 bottom-0 w-full max-w-md bg-white shadow-2xl hidden drawer-enter transition-transform z-50">
    <div class="h-16 px-4 border-b flex items-center justify-between">
      <h3 class="font-semibold text-lg">Your Cart</h3>
      <button onclick="toggleCart(false)" class="p-2" aria-label="Close cart">×</button>
    </div>
    <div id="cartItems" class="flex-1 overflow-auto p-4 space-y-4"></div>
    <div class="border-t p-4 space-y-2">
      <div class="flex justify-between text-sm"><span>Subtotal</span><span id="subtotal">$0.00</span></div>
      <div class="flex justify-between text-sm"><span>Tax</span><span id="tax">$0.00</span></div>
      <div class="flex justify-between font-semibold"><span>Total</span><span id="total">$0.00</span></div>
      <div class="flex gap-2 mt-3">
        <button id="checkout" class="flex-1 rounded-xl px-4 py-3 text-sm font-semibold btn-brand text-white shadow-sm disabled:opacity-50" disabled>Checkout</button>
        <button id="clear" class="rounded-xl px-4 py-3 text-sm font-semibold bg-white border border-slate-200 shadow-sm">Clear</button>
      </div>
    </div>
  </aside>

  <!-- Mock Checkout Modal -->
  <div id="modalOverlay" class="fixed inset-0 bg-black/40 hidden z-50"></div>
  <div id="modal" class="fixed inset-0 grid place-items-center p-4 hidden z-50">
    <div class="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl">
      <h3 class="text-lg font-semibold">Secure Checkout (Demo)</h3>
      <p class="text-sm text-slate-600">This is a mock payment form for demonstration only.</p>
      <form id="pay" class="mt-4 space-y-3">
        <label class="block"><span class="text-xs font-medium text-slate-600">Name on Card</span><input required class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
        <label class="block"><span class="text-xs font-medium text-slate-600">Card</span><input required placeholder="4242 4242 4242 4242" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
        <div class="grid grid-cols-3 gap-2">
          <label class="block"><span class="text-xs font-medium text-slate-600">Exp</span><input required placeholder="MM/YY" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
          <label class="block"><span class="text-xs font-medium text-slate-600">CVC</span><input required placeholder="123" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
          <label class="block"><span class="text-xs font-medium text-slate-600">ZIP</span><input required placeholder="84049" class="w-full rounded-xl border border-slate-300 px-4 py-3 focus-green"></label>
        </div>
        <button class="w-full rounded-xl px-5 py-3 text-sm font-semibold btn-brand text-white shadow-sm">Pay <span id="payTotal">$0.00</span></button>
      </form>
    </div>
  </div>

  <script>
    const MENU = [
      { id:'bbq', name:'BBQ Chicken', category:'Pizza', price:17, desc:'Smoky BBQ sauce, grilled chicken, red onion, cilantro.' },
      { id:'pep', name:'Pepperoni', category:'Pizza', price:16, desc:'Classic pepperoni, mozzarella, house red sauce.' },
      { id:'veg', name:'Veggie', category:'Pizza', price:16, desc:'Bell pepper, mushroom, olive, red onion, basil.' },
      { id:'buff', name:'Buffalo Chicken', category:'Pizza', price:17, desc:'Buffalo sauce, chicken, ranch drizzle, scallions.' },
      { id:'che', name:'Classic Cheese', category:'Pizza', price:14, desc:'Whole-milk mozzarella on our signature red sauce.' },
      { id:'salad', name:'Garden Salad', category:'Salad', price:8,  desc:'Greens, tomato, cucumber, vinaigrette.' },
      { id:'drinks', name:'Fountain Drinks (your choice)', category:'Drinks', price:3, desc:'Coke, Diet, Sprite, root beer, lemonade, etc.' },
      { id:'ranch', name:'Ranch Cup', category:'Dipping', price:1.5, desc:'Buttermilk ranch.' },
      { id:'buff-cup', name:'Buffalo Sauce Cup', category:'Dipping', price:1.5, desc:'Kick of heat.' },
      { id:'garlic', name:'Garlic Oil Cup', category:'Dipping', price:1.5, desc:'Fragrant garlic-infused oil.' }
    ];

    const state = { cart: {}, q:'' };
    const $ = sel => document.querySelector(sel);
    const $$ = sel => document.querySelectorAll(sel);
    const money = v => `$${v.toFixed(2)}`;

    // Mobile nav
    $('#navBtn').onclick = () => $('#mobileNav').classList.toggle('hidden');

    // Smooth anchor scroll
    document.addEventListener('click', (e)=>{
      const a = e.target.closest('a[href^="#"]');
      if(!a) return;
      const id = a.getAttribute('href').slice(1);
      const el = document.getElementById(id);
      if(!el) return;
      e.preventDefault();
      window.scrollTo({ top: el.offsetTop - 80, behavior:'smooth'});
      $('#mobileNav')?.classList.add('hidden');
    });

    // Render menu
    const menuGrid = $('#menuGrid');
    function renderMenu(){
      const items = MENU.filter(m => (m.name+" "+m.desc+" "+m.category).toLowerCase().includes(state.q.toLowerCase()));
      menuGrid.innerHTML = items.map(i => `
        <div class="rounded-2xl border border-slate-200 p-5 shadow-sm bg-white h-full flex flex-col">
          <div class="flex items-start justify-between gap-3">
            <div>
              <h3 class="font-semibold text-lg">${i.name}</h3>
              <p class="text-sm text-slate-500">${i.desc}</p>
            </div>
            <span class="font-bold text-brand">${money(i.price)}</span>
          </div>
          <div class="mt-auto pt-4 flex items-center justify-between">
            <span class="inline-flex items-center text-xs px-2 py-1 rounded-lg bg-slate-50 border border-slate-200">${i.category}</span>
            <button data-add="${i.id}" class="rounded-xl px-4 py-2 text-sm font-semibold btn-brand text-white shadow-sm">Add to Cart</button>
          </div>
        </div>`).join('');
    }
    renderMenu();

    // Search
    $('#search').addEventListener('input', e=>{ state.q = e.target.value; renderMenu(); });

    // Add to cart
    document.addEventListener('click', (e)=>{
      const btn = e.target.closest('[data-add]');
      if(!btn) return;
      const id = btn.getAttribute('data-add');
      state.cart[id] = (state.cart[id]||0)+1;
      updateCartUI();
      toggleCart(true);
    });

    // Cart drawer controls
    const overlay = $('#overlay');
    const drawer = $('#drawer');
    $('#cartBtn').onclick = ()=> toggleCart(true);
    overlay.onclick = ()=> toggleCart(false);

    function toggleCart(show){
      overlay.classList.toggle('hidden', !show);
      drawer.classList.toggle('hidden', !show);
      setTimeout(()=> drawer.classList.toggle('show', show), 0);
    }

    function updateCartUI(){
      const list = Object.entries(state.cart).map(([id, qty])=> ({...MENU.find(m=>m.id===id), qty}));
      $('#cartCount').textContent = list.reduce((s,i)=>s+i.qty,0);
      const subtotal = list.reduce((s,i)=> s + i.price*i.qty, 0);
      const tax = subtotal * 0.0725; const total = subtotal + tax;
      $('#subtotal').textContent = money(subtotal);
      $('#tax').textContent = money(tax);
      $('#total').textContent = money(total);
      $('#payTotal').textContent = money(total);
      $('#checkout').disabled = list.length===0;

      const rows = list.map(i => `
        <div class="flex items-start justify-between gap-3 border-b pb-3">
          <div>
            <div class="font-medium">${i.name}</div>
            <div class="text-sm text-slate-500">${money(i.price)} • ${i.category}</div>
            <div class="mt-2 inline-flex items-center gap-2">
              <label class="text-xs text-slate-500">Qty</label>
              <input type="number" min="0" value="${i.qty}" data-qty="${i.id}" class="w-16 rounded-lg border border-slate-300 px-2 py-1"/>
            </div>
          </div>
          <div class="text-right">
            <div class="font-semibold">${money(i.price*i.qty)}</div>
            <button data-remove="${i.id}" class="text-xs text-rose-600 hover:underline mt-1">Remove</button>
          </div>
        </div>`).join('');
      $('#cartItems').innerHTML = rows || '<p class="text-slate-600">Your cart is empty. Add some pies!</p>';
    }

    // Qty & remove
    document.addEventListener('input', (e)=>{
      const el = e.target.closest('[data-qty]');
      if(!el) return;
      const id = el.getAttribute('data-qty');
      const val = Math.max(0, Number(el.value||0));
      if(val<=0) delete state.cart[id]; else state.cart[id]=val;
      updateCartUI();
    });
    document.addEventListener('click', (e)=>{
      const r = e.target.closest('[data-remove]');
      if(!r) return;
      const id = r.getAttribute('data-remove');
      delete state.cart[id];
      updateCartUI();
    });

    // Clear cart
    $('#clear').onclick = ()=>{ state.cart = {}; updateCartUI(); };

    // Checkout modal
    const modal = $('#modal');
    const modalOverlay = $('#modalOverlay');
    $('#checkout').onclick = ()=>{ modal.classList.remove('hidden'); modalOverlay.classList.remove('hidden'); };
    modalOverlay.onclick = ()=>{ modal.classList.add('hidden'); modalOverlay.classList.add('hidden'); };
    $('#pay').onsubmit = (e)=>{ e.preventDefault(); alert('Order placed! See you soon.'); modal.classList.add('hidden'); modalOverlay.classList.add('hidden'); toggleCart(false); state.cart={}; updateCartUI(); };

    // Forms
    $('#contactForm').onsubmit = (e)=>{ e.preventDefault(); alert('Message sent! We’ll reply soon.'); };
    $('#newsletter').onsubmit = (e)=>{ e.preventDefault(); alert('Subscribed! Check your inbox.'); };

    // Footer year
    $('#year').textContent = new Date().getFullYear();

    // Initialize
    updateCartUI();
    </script>
  <script>
    // --- Robust image path loader ---
    (function(){
      const candidates = [
        'logo.png', './logo.png', '/logo.png', 'images/logo.png', './images/logo.png'
      ];
      const ids = ['headerLogo','bgLogo','heroLogo','footerLogo'];
      function tryLoad(path){
        return new Promise(res=>{ const i = new Image(); i.onload = ()=>res(path); i.onerror=()=>res(null); i.src = path; });
      }
      (async ()=>{
        for (const path of candidates){
          // eslint-disable-next-line no-await-in-loop
          const ok = await tryLoad(path);
          if(ok){
            ids.forEach(id=>{ const el = document.getElementById(id); if(el) el.src = ok; });
            console.log('[Dave\'s Pizza] Using image path:', ok);
            return;
          }
        }
        console.warn("[Dave's Pizza] Could not find logo image. Place your file next to index.html named 'logo.png' or in an /images folder.");
      })();
    })();
  </script>
</body>
</html>